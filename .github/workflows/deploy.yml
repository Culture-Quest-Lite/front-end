name: CI/CD Pipeline

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

env:
  REGISTRY: ghcr.io

jobs:

  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    outputs:
      image_full: ${{ steps.lower.outputs.image_full }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Convert image name to lowercase
        id: lower
        run: |
          IMAGE_FULL="${{ env.REGISTRY }}/${{ github.repository_owner }}/culture-quest-frontend"
          echo "image_full=${IMAGE_FULL,,}" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ steps.lower.outputs.image_full }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=,format=short
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          dockerfile: Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            NEXT_PUBLIC_API_BASE_URL=${{ vars.NEXT_PUBLIC_API_BASE_URL }}
            NEXT_PUBLIC_KEYCLOAK_URL=${{ vars.NEXT_PUBLIC_KEYCLOAK_URL }}
            NEXT_PUBLIC_KEYCLOAK_REALM=${{ vars.NEXT_PUBLIC_KEYCLOAK_REALM }}
            NEXT_PUBLIC_KEYCLOAK_CLIENT_ID=${{ vars.NEXT_PUBLIC_KEYCLOAK_CLIENT_ID }}
            NEXT_PUBLIC_GOONG_API_KEY=${{ secrets.NEXT_PUBLIC_GOONG_API_KEY }}
            NEXT_PUBLIC_GOONG_MAPTILES_KEY=${{ secrets.NEXT_PUBLIC_GOONG_MAPTILES_KEY }}

  deploy:
    name: Deploy to Production Server
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    environment:
      name: production
      url: http://${{ vars.SERVER_HOST }}:3000

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Get runner public IP
        id: runner-ip
        run: echo "ip=$(curl -s https://checkip.amazonaws.com)" >> "$GITHUB_OUTPUT"

      - name: Whitelist runner IP on Security Group (open SSH)
        run: |
          aws ec2 authorize-security-group-ingress \
            --group-id "${{ secrets.SG_ID }}" \
            --protocol tcp \
            --port ${{ secrets.SERVER_PORT }} \
            --cidr "${{ steps.runner-ip.outputs.ip }}/32" \
            || echo "Ingress rule already exists, continuing."

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          IMAGE_FULL: ${{ needs.build-and-push.outputs.image_full }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_ACTOR: ${{ github.actor }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          envs: IMAGE_FULL,GH_TOKEN,GH_ACTOR
          script: |
            set -e

            cd ${{ secrets.FRONTEND_SERVER_DEPLOY_DIR }}

            echo "==> Fetching latest code and resetting to ${{ github.ref_name }}..."
            git fetch origin
            git reset --hard
            git checkout ${{ github.ref_name }} || git checkout -b ${{ github.ref_name }} origin/${{ github.ref_name }}
            git reset --hard origin/${{ github.ref_name }}

            echo "==> Logging in to GitHub Container Registry..."
            echo "$GH_TOKEN" | docker login ghcr.io -u "$GH_ACTOR" --password-stdin

            # Determine tag name (latest for main, branch name otherwise)
            export IMAGE_TAG="${{ github.ref_name }}"
            if [ "$IMAGE_TAG" = "main" ]; then
              export IMAGE_TAG="latest"
            fi

            echo "==> Injecting IMAGE_FULL and IMAGE_TAG into .env..."
            # Remove old IMAGE_FULL/IMAGE_TAG lines if present, then append new values
            sed -i '/^IMAGE_FULL=/d' .env
            sed -i '/^IMAGE_TAG=/d' .env
            echo "IMAGE_FULL=$IMAGE_FULL" >> .env
            echo "IMAGE_TAG=$IMAGE_TAG" >> .env

            echo "==> Pulling new Docker image: $IMAGE_FULL:$IMAGE_TAG"
            docker pull "$IMAGE_FULL:$IMAGE_TAG"

            echo "==> Recreating frontend container..."
            docker compose up -d --no-deps --no-build frontend

            echo "==> Cleaning up unused images..."
            docker image prune -f

            echo "==> Deployment complete!"
            docker compose ps

      - name: Revoke runner IP from Security Group (close SSH)
        if: always()
        run: |
          aws ec2 revoke-security-group-ingress \
            --group-id "${{ secrets.SG_ID }}" \
            --protocol tcp \
            --port ${{ secrets.SERVER_PORT }} \
            --cidr "${{ steps.runner-ip.outputs.ip }}/32" \
            || echo "Nothing to revoke, continuing."
